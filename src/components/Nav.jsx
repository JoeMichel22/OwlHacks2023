const Nav = () => {
  return (
    <header className=" w-full">
      <nav className="w-full flex justify-center font-bold">
        <span>Museums in Philadelphia</span>
      </nav>
    </header>
  );
};

export default Nav;
